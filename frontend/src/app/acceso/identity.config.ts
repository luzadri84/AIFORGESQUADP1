import { AzureIdentitySettings } from './azure-identity';
// Basic is independent of Azure. Populate only after an authorized tenant/app integration.
export const azureIdentity: AzureIdentitySettings | undefined = undefined;
