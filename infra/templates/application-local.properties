# Merge into the real starter only after inspecting its existing security/configuration.
spring.datasource.url=${JDBC_URL}
spring.datasource.username=${JDBC_USER}
spring.datasource.password=${JDBC_PASSWORD}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
server.port=8080
# Basic and CSRF must remain enabled by the real application's security configuration.